#pragma once
/**
 * Plantilla de credenciales. Copiar como `secrets.h` y completar.
 *
 *   cp src/secrets.h.example src/secrets.h
 *
 * `secrets.h` esta en .gitignore: NUNCA se versiona (CLAUDE.md 5 y 8).
 */

// --- Wi-Fi (Hito 6a) ---
// Solo 2.4 GHz: el ESP32-C3 no tiene 5 GHz.
#define WIFI_SSID  "TU_SSID_2G"
#define WIFI_PASS  "TU_PASSWORD"

// --- MQTT (Hito 6b - todavia no se usa) ---
#define MQTT_BROKER "192.168.0.100"   // IP del Edge Gateway (Mosquitto)
#define MQTT_PORT   1883              // VLAN 10 solo permite este puerto
#define MQTT_SALA   "sala1"           // segmento {sala} del topico
